#!/usr/bin/env bash

set -e +o pipefail

# npm test && bash <(curl -s https://codecov.io/bash) && npm run lint -s
nyc npm test && nyc report --reporter=text-lcov | coveralls && bash <(curl -s https://codecov.io/bash) && npm run lint -s