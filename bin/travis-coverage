#!/usr/bin/env bash

set -e +o pipefail

# Run instide Docker container
nyc npm test && nyc report --reporter=text-lcov | coveralls | codeclimate-test-reporter && bash <(curl -s https://codecov.io/bash) && npm run lint -s